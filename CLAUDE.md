# Claude adapter for agentos

Claude-specific shim. The provider-agnostic truth is in `AGENTS.md`; this file just
imports it so Claude loads the same content. Add Claude-only notes below the import if
ever needed — keep knowledge in `AGENTS.md`, not here.

@AGENTS.md
